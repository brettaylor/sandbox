spring:
  application:
    name: petstore
  
  jackson:
    deserialization:
      accept-single-value-as-array: true

        
  datasource:
    # JDBC URL format: jdbc:postgresql://<host>:<port>/<database_name>
    url: jdbc:h2:mem:file:~/petstore;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
    hikari:
      jdbc-url: ${spring.datasource.url}
      username: ${spring.datasource.username}
      password: ${spring.datasource.password}
      driver-class-name: ${spring.datasource.driver-class-name}

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      # Options: none, validate, update, create, create-drop
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true
springdoc:
  # Path for the JSON OpenAPI specification (Default: /v3/api-docs)
  api-docs:
    enabled: true
    
  # Path for the Swagger HTML UI (Default: /swagger-ui.html)
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    # Persist authorization data (JWT/API Keys) upon page refresh
    persistAuthorization: true
    # Sort endpoints alphabetically (can also be 'method')
    operationsSorter: alpha 

server:
  address: localhost
  port: 8080
  servlet:
    context-path: /
logging:
  level:
    root: info
